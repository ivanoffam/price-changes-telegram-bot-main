telegram:
  botName: YOUR_TELEGRAM_BOT_NAME
  chatId: 123
  botToken: YOUR_TELEGRAM_BOT_TOKEN
crypto:
  apiMexcUrl: https://api.mexc.com/api/v3/ticker/price
  priceChangeThreshold: 5
  refreshIntervalSeconds: 60
  maxUsers: 10
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
jpa:
  hibernate:
    ddl-auto: create
  show-sql: true
